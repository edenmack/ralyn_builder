DOC: sample ralyn program
TODO: {
	VAR:	define data or struct types
	OBJECT: data objects
	FN:		subroutine definition
	LIST:	define lists or array types
	LOOP:	control structure
	TEST:	control structure
}

SEC: main{
	VAR: string vars for printing {
		s1<string> = "hello world";
		s2<string> = $("hello programmer");
		s3<string>; //string is not initiated... null value or empty?
	}

	//print() -> Console.WriteLine()... for now at least
	print(s1);
	print(s2);
}